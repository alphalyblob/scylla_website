<div class="disp-flex-around color-white">
    <img src="{{ asset('img/sc-logo-noir-transparent.png') }}" class="footer-img" alt="Scylla Logo"/>
    <h1 class="bg-scylla font-oswald color-white">Bienvenue chez Scylla - Arts De La Scène !</h1>
</div>

<div class="font-oswald">
    <h3> Bonjour à toi nouvel adhérent </h3>
    <h4>Nous sommes très heureux.ses de te compter parmi nous !</h4>
    <p>Tu peux désormais accéder à ton compte en te connectant avec tes identifiants ci-dessous : </p>
    <p>Ton identifiant : {{ adherent.email }}</p>
    <p>Ton mot de passe <p class="danger-message">TEMPORAIRE</p> : {{adherent.password}}</p>

    <p>Tu es invité à te connecter une première fois en suivant ce lien : </p>
    <a href="{{ url('app_login') }}">{{ url('app_login') }}</a>

    {# <a href="{{ url('app_reset_password', {token: resetToken.token}) }}">{{ url('app_reset_password', {token: resetToken.token}) }}</a> #}

    {# <p>This link will expire in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p> #}
    
    <p class="danger-message">N'oublie pas de modifier ton mot de passe !</p>
</div>