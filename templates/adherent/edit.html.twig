{% extends 'base.html.twig' %}

{% block title %}Scylla - Profil{% endblock %}

{% block body %}

<div class="container bg-scylla font-oswald color-white position-form">

    <div class="form-box">
        <h1>Modifier mon mot de passe</h1>

        {{ include('adherent/_form.html.twig', {'button_label': 'Modifier'}) }}

        {# <a href="{{ path('app_adherent_show, {'id': adherent.id}') }}">Retour</a> #}

        {# {{ include('adherent/_delete_form.html.twig') }} #}
    </div>
</div>
{% endblock %}
